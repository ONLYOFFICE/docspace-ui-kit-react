<!-- ui-kit-doc {
  "schema": 1,
  "name": "Table",
  "folder": "components/table",
  "kind": "compound",
  "category": "Data display",
  "status": "public",
  "summary": "Columnar list with resizable, sortable and hideable columns, laid out by a CSS grid the header writes.",
  "import": { "subpath": "components/table", "barrel": true, "default": false },
  "exports": ["TableContainer", "TableHeader", "TableBody", "TableRow", "TableCell", "TableGroupMenu", "TTableColumn", "TGroupMenuItem"],
  "providers": ["ThemeProvider", "TranslationProvider"],
  "state": { "visibility": null, "close": "onHideContextMenu", "loading": null, "disabled": "isBlocked" },
  "related": ["rows", "context-menu", "checkbox"],
  "subComponents": ["TableContainer", "TableHeader", "TableBody", "TableRow", "TableCell", "TableGroupMenu"],
  "testIds": ["table-container", "table-header", "table-body", "table-row", "table-cell", "table-group-menu", "table-settings"]
} -->

# Table

Columnar list with resizable, sortable and hideable columns, laid out by a CSS grid the header
writes. Six components that only work together, and only one table to a page.

## Use this when / not when

- Use for data the user reads across and sorts: files, members, sessions, anything with columns
  worth resizing and hiding.
- Not for a list of items with one line each — [`Rows`](../rows/README.md) is the portal's own
  list, and a flex column of your own is smaller than either.
- Not for a short, fixed table. This one persists column widths in `localStorage`, measures the
  container and rewrites a grid on every resize; a `<table>` element of your own does not.
- Not twice on one page. The container's id, the header's id and the column ids are all
  literal.

## Import

```ts
import {
  TableBody,
  TableCell,
  TableContainer,
  TableHeader,
  TableRow,
} from "@onlyoffice/apps-ui-kit/components/table";
```

Also exported from the root barrel `@onlyoffice/apps-ui-kit`.

None of the props types are exported either — `TTableColumn` and `TGroupMenuItem` are, and they
are the two you write by hand.

Needs `ThemeProvider` from `@onlyoffice/apps-ui-kit/providers/theme`, and
`TranslationProvider` from `@onlyoffice/apps-ui-kit/providers/translation` for the group menu's
own labels.

## Minimal example

The header needs the container's ref to write the grid on it, and a `columnStorageName` to save
the widths under.

```tsx
import { useRef } from "react";
import {
  TableBody,
  TableCell,
  TableContainer,
  TableHeader,
  type TTableColumn,
} from "@onlyoffice/apps-ui-kit/components/table";

const COLUMNS: TTableColumn[] = [
  { key: "name", title: "Name", enable: true, resizable: true },
  { key: "role", title: "Role", enable: true },
];

export function MemberTable({ members }: { members: string[] }) {
  const container = useRef<HTMLDivElement>(null);

  return (
    <TableContainer forwardedRef={container} useReactWindow={false}>
      <TableHeader
        containerRef={container}
        columns={COLUMNS}
        columnStorageName="membersTableColumns"
        columnInfoPanelStorageName="membersTableColumnsInfoPanel"
        sectionWidth={960}
        useReactWindow={false}
        showSettings={false}
      />
      <TableBody
        columnStorageName="membersTableColumns"
        columnInfoPanelStorageName="membersTableColumnsInfoPanel"
        filesLength={members.length}
        itemCount={members.length}
        itemHeight={41}
        hasMoreFiles={false}
        useReactWindow={false}
        fetchMoreFiles={() => Promise.resolve()}
      >
        {members.map((name) => (
          <TableCell key={name}>{name}</TableCell>
        ))}
      </TableBody>
    </TableContainer>
  );
}
```

## Props

Each part has its own props. `TTableColumn`, the shape of an entry in `columns`, is where most
of the table's behaviour is configured.

### `TableContainer`

<!-- props:start TableContainerProps -->

_Generated by `pnpm readme:props` from `TableContainerProps` in `Table.types.ts`. Do not edit; edit the JSDoc._

| Prop             | Type                        | Required | Default | Description                                                              |
| ---------------- | --------------------------- | -------- | ------- | ------------------------------------------------------------------------ |
| `forwardedRef`   | `React.Ref<HTMLDivElement>` | **yes**  | –       | Ref of the container element, which the header needs to set the grid on. |
| `useReactWindow` | `boolean`                   | **yes**  | –       | Whether the body inside is virtualised. It only switches a class here.   |
| `children`       | `React.ReactNode`           | no       | –       | The header, the group menu and the body.                                 |
| `className`      | `string`                    | no       | –       | Applied to the container.                                                |
| `noSelect`       | `boolean`                   | no       | –       | Disables text selection inside the table.                                |

<!-- props:end -->

### `TableHeader`

<!-- props:start TableHeaderProps -->

_Generated by `pnpm readme:props` from `TableHeaderProps` in `Table.types.ts`. Do not edit; edit the JSDoc._

| Prop                         | Type                                                                     | Required | Default | Description                                                                                                                                       |
| ---------------------------- | ------------------------------------------------------------------------ | -------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `columns`                    | `TTableColumn[]`                                                         | **yes**  | –       | The columns, in order. The header also calls a column's `onChange` itself when the table is sorted by a column that is not enabled.               |
| `columnStorageName`          | `string`                                                                 | **yes**  | –       | `localStorage` key the column widths are saved under. It is required and has to be unique per table, or two tables overwrite each other's layout. |
| `containerRef`               | `{ current: HTMLDivElement \| null; }`                                   | **yes**  | –       | Ref of the `TableContainer`, whose `grid-template-columns` this component writes.                                                                 |
| `sectionWidth`               | `number`                                                                 | **yes**  | –       | Width of the section around the table, in pixels.                                                                                                 |
| `showSettings`               | `boolean`                                                                | **yes**  | `true`  | Whether the settings cog is rendered at the end of the header.                                                                                    |
| `useReactWindow`             | `boolean`                                                                | **yes**  | `false` | Whether the body is virtualised, which changes how the rows are re-laid out.                                                                      |
| `columnInfoPanelStorageName` | `string`                                                                 | no       | –       | `localStorage` key used instead of `columnStorageName` while the info panel is open.                                                              |
| `infoPanelVisible`           | `boolean`                                                                | no       | –       | Tells the header to use the info-panel storage key and its narrower layout.                                                                       |
| `isIndexEditingMode`         | `boolean`                                                                | no       | –       | Disables the settings cog while the rows are being reordered.                                                                                     |
| `isLengthenHeader`           | `boolean`                                                                | no       | –       | Lets the header run past the table's width.                                                                                                       |
| `onClick`                    | `() => void`                                                             | no       | –       | Called on a click on the header.                                                                                                                  |
| `resetColumnsSize`           | `boolean`                                                                | no       | –       | Discards the saved widths and measures the columns again.                                                                                         |
| `setHideColumns`             | `(value: boolean) => void`                                               | no       | –       | Called when the header runs out of room and hides its columns.                                                                                    |
| `settingsTitle`              | `string`                                                                 | no       | –       | Hover tooltip of the settings cog.                                                                                                                |
| `sortBy`                     | `string`                                                                 | no       | –       | Field the table is sorted by; the matching column is highlighted.                                                                                 |
| `sorted`                     | `boolean`                                                                | no       | –       | Whether the sort is descending, which turns the arrow.                                                                                            |
| `sortingVisible`             | `boolean`                                                                | no       | `true`  | Whether the sort arrows are rendered and the cells react to a click.                                                                              |
| `style`                      | `React.CSSProperties`                                                    | no       | –       | Ignored. Nothing reads this prop.                                                                                                                 |
| `tagRef`                     | `((node: HTMLDivElement) => void) \| React.ForwardedRef<HTMLDivElement>` | no       | –       | Ref handed to the cell of the column that asks for it with `withTagRef`.                                                                          |
| `withoutWideColumn`          | `boolean`                                                                | no       | –       | Stops the last column being stretched to fill the leftover width.                                                                                 |

<!-- props:end -->

### `TableBody`

<!-- props:start TableBodyProps -->

_Generated by `pnpm readme:props` from `TableBodyProps` in `Table.types.ts`. Do not edit; edit the JSDoc._

| Prop                         | Type                                    | Required | Default | Description                                                                                                                                           |
| ---------------------------- | --------------------------------------- | -------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `children`                   | `React.ReactNode[]`                     | **yes**  | –       | The rows. It must be an array, one entry per row.                                                                                                     |
| `columnStorageName`          | `string`                                | **yes**  | –       | `localStorage` key of the column widths. The body renders **nothing but an empty element** unless this and `columnInfoPanelStorageName` are both set. |
| `fetchMoreFiles`             | `(params: IndexRange) => Promise<void>` | **yes**  | –       | Called with the range to load when the user scrolls near the end.                                                                                     |
| `filesLength`                | `number`                                | **yes**  | –       | How many rows are loaded so far.                                                                                                                      |
| `hasMoreFiles`               | `boolean`                               | **yes**  | –       | Whether there is another page to ask for.                                                                                                             |
| `itemCount`                  | `number`                                | **yes**  | –       | How many rows there are in total.                                                                                                                     |
| `itemHeight`                 | `number`                                | **yes**  | `41`    | Height of one row in pixels, the same for every row.                                                                                                  |
| `useReactWindow`             | `boolean`                               | **yes**  | `true`  | Whether the rows are virtualised.                                                                                                                     |
| `columnInfoPanelStorageName` | `string`                                | no       | –       | The info-panel variant of that key. It is as required as the other one.                                                                               |
| `infoPanelVisible`           | `boolean`                               | no       | `false` | Narrows the body for an open info panel.                                                                                                              |
| `isIndexEditingMode`         | `boolean`                               | no       | –       | Passed through while the rows are being reordered.                                                                                                    |
| `onScroll`                   | `() => void`                            | no       | –       | Called as the list scrolls. Only with virtualisation on.                                                                                              |

<!-- props:end -->

### `TableRow`

<!-- props:start TableRowProps -->

_Generated by `pnpm readme:props` from `TableRowProps` in `Table.types.ts`. Do not edit; edit the JSDoc._

| Prop                   | Type                                      | Required | Default       | Description                                                                                                               |
| ---------------------- | ----------------------------------------- | -------- | ------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `children`             | `React.ReactNode`                         | **yes**  | –             | The row's cells, normally `TableCell`s, one per column.                                                                   |
| `badgeUrl`             | `string`                                  | no       | –             | URL of the badge image shown in the context menu's header.                                                                |
| `checked`              | `boolean`                                 | no       | –             | Highlights the row as selected. There is no checkbox here — put one in a cell.                                            |
| `className`            | `string`                                  | no       | –             | Applied to the row.                                                                                                       |
| `contextMenuCellStyle` | `React.CSSProperties`                     | no       | –             | Applied to the cell that holds the context button.                                                                        |
| `contextMenuTestId`    | `string`                                  | no       | –             | Value of `data-testid` on the context menu.                                                                               |
| `contextOptions`       | `ContextMenuModel[]`                      | no       | –             | Items of the context menu. Its **presence** is what renders the context button — an empty array renders a spacer instead. |
| `dataTestId`           | `string`                                  | no       | `"table-row"` | Value of `data-testid` on the row.                                                                                        |
| `dragging`             | `boolean`                                 | no       | –             | Dims the row while it is being dragged.                                                                                   |
| `fileContextClick`     | `(value?: boolean) => void`               | no       | –             | Called when the context menu is asked for, with `true` for a right-click.                                                 |
| `forwardedRef`         | `React.ForwardedRef<HTMLDivElement>`      | no       | –             | Ref of the row element.                                                                                                   |
| `getContextModel`      | `() => ContextMenuModel[]`                | no       | –             | Builds the context menu's items when it opens.                                                                            |
| `hideColumns`          | `boolean`                                 | no       | –             | Applies the narrow layout the header asks for when it runs out of room.                                                   |
| `isActive`             | `boolean`                                 | no       | –             | Highlights the row as the one the context menu belongs to.                                                                |
| `isIndexEditingMode`   | `boolean`                                 | no       | –             | Hides the context button while the rows are being reordered.                                                              |
| `onClick`              | `(e: React.MouseEvent) => void`           | no       | –             | Called on a click anywhere in the row.                                                                                    |
| `onDoubleClick`        | `(e: React.MouseEvent) => void`           | no       | –             | Called on a double click anywhere in the row.                                                                             |
| `onHideContextMenu`    | `() => void`                              | no       | –             | Called when the context menu closes.                                                                                      |
| `onMouseEnter`         | `(e: React.MouseEvent) => void`           | no       | –             | Called when the pointer enters the row.                                                                                   |
| `onMouseLeave`         | `(e: React.MouseEvent) => void`           | no       | –             | Called when the pointer leaves the row.                                                                                   |
| `selectionProp`        | `{ className?: string; value?: string; }` | no       | –             | Class and value spread onto the cell that holds the context button.                                                       |
| `style`                | `React.CSSProperties`                     | no       | –             | Applied to the row. The header overwrites the grid columns of every row.                                                  |
| `title`                | `string`                                  | no       | –             | Hover tooltip of the context button.                                                                                      |

<!-- props:end -->

### `TableCell`

<!-- props:start TableCellProps -->

_Generated by `pnpm readme:props` from `TableCellProps` in `Table.types.ts`. Do not edit; edit the JSDoc._

| Prop            | Type                                 | Required | Default        | Description                                                               |
| --------------- | ------------------------------------ | -------- | -------------- | ------------------------------------------------------------------------- |
| `checked`       | `boolean`                            | no       | –              | Applies the selected styling.                                             |
| `children`      | `React.ReactNode`                    | no       | –              | Content of the cell.                                                      |
| `className`     | `string`                             | no       | –              | Applied to the cell.                                                      |
| `dataTestId`    | `string`                             | no       | `"table-cell"` | Value of `data-testid` on the cell.                                       |
| `documentTitle` | `string`                             | no       | –              | Written to the element as `data-document-title`.                          |
| `forwardedRef`  | `React.ForwardedRef<HTMLDivElement>` | no       | –              | Ref of the cell element.                                                  |
| `hasAccess`     | `boolean`                            | no       | –              | Applies the styling of a cell whose row the user may act on.              |
| `style`         | `React.CSSProperties`                | no       | –              | Applied to the cell.                                                      |
| `value`         | `string`                             | no       | –              | Written to the element as a `value` attribute, which drag and drop reads. |

<!-- props:end -->

### `TableGroupMenu`

<!-- props:start TableGroupMenuProps -->

_Generated by `pnpm readme:props` from `TableGroupMenuProps` in `Table.types.ts`. Do not edit; edit the JSDoc._

| Prop                      | Type                                                                                              | Required | Default | Description                                                                    |
| ------------------------- | ------------------------------------------------------------------------------------------------- | -------- | ------- | ------------------------------------------------------------------------------ |
| `headerMenu`              | `TGroupMenuItem[]`                                                                                | **yes**  | –       | The buttons of the menu, in order.                                             |
| `isChecked`               | `boolean`                                                                                         | **yes**  | –       | Whether the select-all checkbox is ticked.                                     |
| `isIndeterminate`         | `boolean`                                                                                         | **yes**  | –       | Whether it is drawn as partly ticked.                                          |
| `onChange`                | `(isChecked: boolean) => void`                                                                    | **yes**  | –       | Called with the new state when the select-all checkbox changes.                |
| `withoutInfoPanelToggler` | `boolean`                                                                                         | **yes**  | –       | Whether the info-panel button at the end is left out.                          |
| `checkboxMargin`          | `string`                                                                                          | no       | –       | Margin around that checkbox, as a CSS length.                                  |
| `checkboxOptions`         | `React.ReactElement<{ children?: React.ReactNode; }, string \| React.JSXElementConstructor<any>>` | no       | –       | Element whose children become the options of the selection combo box.          |
| `headerLabel`             | `string`                                                                                          | no       | –       | Text shown instead of the select-all checkbox.                                 |
| `isBlocked`               | `boolean`                                                                                         | no       | –       | Greys every button out.                                                        |
| `isCloseable`             | `boolean`                                                                                         | no       | –       | Whether a close cross is rendered. It comes with `onCloseClick` or not at all. |
| `isInfoPanelVisible`      | `boolean`                                                                                         | no       | –       | Colours that button as active.                                                 |
| `isMobileView`            | `boolean`                                                                                         | no       | –       | Lays the buttons out for a phone.                                              |
| `onClick`                 | `() => void`                                                                                      | no       | –       | Called on a click anywhere in the menu.                                        |
| `onCloseClick`            | `() => void`                                                                                      | no       | –       | Called by that cross.                                                          |
| `toggleInfoPanel`         | `() => void`                                                                                      | no       | –       | Called when the info-panel button is clicked.                                  |
| `withComboBox`            | `boolean`                                                                                         | no       | `true`  | Whether the selection combo box is rendered next to the checkbox.              |

<!-- props:end -->

### `TTableColumn`

<!-- props:start TTableColumn -->

_Generated by `pnpm readme:props` from `TTableColumn` in `Table.types.ts`. Do not edit; edit the JSDoc._

| Prop          | Type                                                                                                         | Required | Default | Description                                                                                                                             |
| ------------- | ------------------------------------------------------------------------------------------------------------ | -------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `key`         | `string`                                                                                                     | **yes**  | –       | Identifier of the column, used as its React key and in the settings menu.                                                               |
| `title`       | `string`                                                                                                     | **yes**  | –       | Text in the header cell. It is rendered only while `enable` is true.                                                                    |
| `active`      | `boolean`                                                                                                    | no       | –       | Highlights the header cell without sorting by it.                                                                                       |
| `checkbox`    | `{ value: boolean; isIndeterminate: boolean; onChange: (e?: React.ChangeEvent<HTMLInputElement>) => void; }` | no       | –       | Renders a checkbox before the title, shown only while it is ticked or indeterminate.                                                    |
| `default`     | `boolean`                                                                                                    | no       | –       | Marks the column as one of the set restored on a reset.                                                                                 |
| `defaultSize` | `number`                                                                                                     | no       | –       | Width the column is reset to, in pixels.                                                                                                |
| `enable`      | `boolean`                                                                                                    | no       | –       | Whether the column is shown. A hidden column keeps its place in the grid.                                                               |
| `isDisabled`  | `boolean`                                                                                                    | no       | –       | Keeps the column out of the settings menu.                                                                                              |
| `isShort`     | `boolean`                                                                                                    | no       | –       | Renders the cell in its narrow form.                                                                                                    |
| `minWidth`    | `number`                                                                                                     | no       | –       | Narrowest the column may be dragged, in pixels. The default is 110, or 210 for the first column.                                        |
| `onChange`    | `(key: string, e?: React.ChangeEvent<HTMLInputElement>) => void`                                             | no       | –       | Called with the column's key when it is ticked in the settings menu. A column without this callback is not offered in that menu at all. |
| `onClick`     | `(sortBy: string, e: React.MouseEvent) => void`                                                              | no       | –       | Called when the header cell is clicked, with `sortBy` and the event.                                                                    |
| `onIconClick` | `() => void`                                                                                                 | no       | –       | Called when the sort arrow is clicked, instead of `onClick`.                                                                            |
| `resizable`   | `boolean`                                                                                                    | no       | –       | Whether the _next_ column may be dragged by this one's right edge.                                                                      |
| `sortBy`      | `string`                                                                                                     | no       | –       | Field this column sorts by; the header compares it with its own `sortBy`.                                                               |
| `withTagRef`  | `boolean`                                                                                                    | no       | –       | Hands the header's `tagRef` to this column's cell.                                                                                      |

<!-- props:end -->

## Recipes

### Sorting

A column sorts by calling back: the table does not reorder anything, and the arrow follows
`sortBy` and `sorted` on the header.

```tsx
import { useRef, useState } from "react";
import {
  TableContainer,
  TableHeader,
  type TTableColumn,
} from "@onlyoffice/apps-ui-kit/components/table";

export function SortableHeader() {
  const container = useRef<HTMLDivElement>(null);
  const [sortBy, setSortBy] = useState("name");
  const [descending, setDescending] = useState(false);

  const columns: TTableColumn[] = [
    {
      key: "name",
      title: "Name",
      enable: true,
      sortBy: "name",
      resizable: true,
      onClick: (field) => {
        setDescending(field === sortBy ? !descending : false);
        setSortBy(field);
      },
    },
    {
      key: "date",
      title: "Modified",
      enable: true,
      sortBy: "date",
      onClick: (field) => setSortBy(field),
    },
  ];

  return (
    <TableContainer forwardedRef={container} useReactWindow={false}>
      <TableHeader
        containerRef={container}
        columns={columns}
        sortBy={sortBy}
        sorted={descending}
        columnStorageName="filesTableColumns"
        columnInfoPanelStorageName="filesTableColumnsInfoPanel"
        sectionWidth={960}
        useReactWindow={false}
        showSettings
      />
    </TableContainer>
  );
}
```

### Selection, with the group menu

The group menu appears in place of the header while something is selected; the select-all
checkbox is its own, and the per-row checkbox is yours to put in a cell.

```tsx
import { useState } from "react";
import { Checkbox } from "@onlyoffice/apps-ui-kit/components/checkbox";
import {
  TableCell,
  TableGroupMenu,
  TableRow,
} from "@onlyoffice/apps-ui-kit/components/table";

export function SelectableRows({ names }: { names: string[] }) {
  const [selected, setSelected] = useState<string[]>([]);

  const toggle = (name: string) =>
    setSelected((current) =>
      current.includes(name)
        ? current.filter((item) => item !== name)
        : [...current, name],
    );

  return (
    <>
      {selected.length > 0 ? (
        <TableGroupMenu
          isChecked={selected.length === names.length}
          isIndeterminate={
            selected.length > 0 && selected.length < names.length
          }
          withoutInfoPanelToggler
          withComboBox={false}
          headerMenu={[
            {
              id: "delete",
              label: "Delete",
              title: "Delete",
              iconUrl: "",
              disabled: false,
              onClick: () => setSelected([]),
            },
          ]}
          onChange={(checked) => setSelected(checked ? names : [])}
        />
      ) : null}
      {names.map((name) => (
        <TableRow key={name} checked={selected.includes(name)}>
          <TableCell>
            <Checkbox
              isChecked={selected.includes(name)}
              onChange={() => toggle(name)}
            />
          </TableCell>
          <TableCell>{name}</TableCell>
        </TableRow>
      ))}
    </>
  );
}
```

## Behaviour the types don't state

- **The header lays out the whole table.** It computes a `grid-template-columns` string and
  writes it onto the container element and onto every `.table-container_row` in the document.
  Cells have no widths of their own, and a row rendered outside that container is not laid out
  at all.
- **`TableBody` renders an empty element unless both storage names are set.** The guard is
  `if (!columnStorageName || !columnInfoPanelStorageName) return <div />`, so a table missing
  the info-panel key shows nothing — no error, no rows. Pass both, always.
- **Column widths live in `localStorage`** under `columnStorageName`, or
  `columnInfoPanelStorageName` while `infoPanelVisible` is set. Two tables sharing a key
  overwrite each other's layout, and a stored string whose column count no longer matches makes
  the header reset the widths.
- **The ids are literal.** The container is `#table-container`, the header
  `#table-container_caption-header`, and each header cell `#column_<index>`; the virtual list
  measures the first of those by id. One table to a page.
- **Only columns with an `onChange` appear in the settings menu**, and `isDisabled` keeps a
  column out of it. A column whose `enable` is false keeps its slot in the grid but renders no
  title.
- **`resizable` on a column describes its neighbour.** The handle is drawn on a cell when the
  _next_ column is resizable, and dragging it moves the boundary between the two. The first
  column cannot go below 210px, the rest below 110px or their own `minWidth`.
- **`TableRow` renders the context button from the presence of `contextOptions`**, like
  [`Row`](../rows/row/README.md), and the button is a
  [`ContextMenuButton`](../context-menu-button/README.md) in `toggle` mode, so the row's own
  right-click handler is what opens the menu.
- The header watches the container with a `ResizeObserver` as well as the window, so it
  re-lays out when a panel opens beside it.
- `useReactWindow` defaults to `false` on the header and `true` on the body; set it the same on
  both and on the container, or the header lays out rows the body never renders.
- `TableCell` writes its `value` prop to the element as a non-standard `value` attribute, which
  drag and drop reads.
- The group menu needs translations of its own for the select-all checkbox and the selection
  combo box.

## Sub-components

| Export           | What it is                                                                       |
| ---------------- | -------------------------------------------------------------------------------- |
| `TableContainer` | The grid the header writes to. Give it a ref and hand the same ref to the header |
| `TableHeader`    | The column titles, the resize handles, the sorting and the settings cog          |
| `TableBody`      | The rows, virtualised and paged in when `useReactWindow` is on                   |
| `TableRow`       | One row: your cells, plus the context menu at the end                            |
| `TableCell`      | One cell. It has no width of its own — the grid decides                          |
| `TableGroupMenu` | The bar that replaces the header while rows are selected                         |

## CSS variables

The table styles itself from the theme and exposes no variables of its own; the column widths
are written as inline `grid-template-columns`, and the group menu reads
`--table-group-menu-checkbox-margin` from its `checkboxMargin` prop.

## Accessibility

- Nothing here is a table to assistive technology: every part is a `<div>`, with no `role`,
  no `columnheader`, no `aria-sort` and no row or column count. A screen reader is given a pile
  of text.
- Sorting is a click on a `<div>`; the sort arrow is an
  [`IconButton`](../icon-button/README.md), which is not focusable either. There is no keyboard
  route to sorting, resizing or the settings menu.
- Selection is whatever [`Checkbox`](../checkbox/README.md) you put in a cell — a real input,
  and the only reachable control in a row.
- Data a keyboard or screen-reader user has to work through is better served by a real
  `<table>` of your own, styled with the kit's `Text`.

## Test ids

| Element          | `data-testid`                                        |
| ---------------- | ---------------------------------------------------- |
| The container    | `table-container`                                    |
| The header       | `table-header`                                       |
| A header cell    | `column-<key>`                                       |
| The settings cog | `table-settings`, its button `table-settings-button` |
| The body         | `table-body`                                         |
| A row            | `table-row`, overridable with `dataTestId`           |
| A cell           | `table-cell`, overridable with `dataTestId`          |
| The group menu   | `table-group-menu`                                   |

## Related

- [`Rows`](../rows/README.md) — the portal's one-line-per-item list.
- [`ContextMenu`](../context-menu/README.md) — the menu every row renders.
- [`Checkbox`](../checkbox/README.md) — what selection is actually made of.
