export enum ComboBoxDisplayType {
  default = "default",
  toggle = "toggle",
}

export enum ComboBoxSize {
  base = "base",
  middle = "middle",
  big = "big",
  huge = "huge",
  content = "content",
}
