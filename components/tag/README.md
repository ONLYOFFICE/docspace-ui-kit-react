<!-- ui-kit-doc {
  "schema": 1,
  "name": "Tag",
  "folder": "components/tag",
  "kind": "component",
  "category": "Data display",
  "status": "public",
  "summary": "Small outlined label for one keyword, clickable and optionally removable.",
  "import": { "subpath": "components/tag", "barrel": true, "default": false },
  "exports": ["Tag", "TagProps", "TagType", "TagClickEvent"],
  "providers": ["ThemeProvider"],
  "state": { "visibility": null, "close": null, "loading": null, "disabled": "isDisabled" },
  "related": ["tags", "selected-item", "badge"],
  "subComponents": [],
  "testIds": ["tag_item"]
} -->

# Tag

Small outlined label for one keyword, clickable and optionally removable. It is the room tag
of the portal, and the piece [`Tags`](../tags/README.md) lays out in a row.

## Use this when / not when

- Use for a keyword attached to an object — a room tag, a category, a provider — that the
  reader may click to filter by.
- Not for several tags side by side: [`Tags`](../tags/README.md) measures the row, collapses
  the overflow into a menu and passes each tag the width it may take.
- Not for something the user chose and can undo — [`SelectedItem`](../selected-item/README.md)
  is the chip with a cross for that.
- Not for a count or a status word; [`Badge`](../badge/README.md) is the filled pill.

## Import

```ts
import { Tag } from "@onlyoffice/apps-ui-kit/components/tag";
```

Also exported from the root barrel `@onlyoffice/apps-ui-kit`.

Needs `ThemeProvider` from `@onlyoffice/apps-ui-kit/providers/theme`; without it the tag has no
border and no colours.

## Minimal example

`onClick` is handed an object, not the DOM event.

```tsx
import { Tag } from "@onlyoffice/apps-ui-kit/components/tag";

export function RoomTag({ onFilter }: { onFilter: (label: string) => void }) {
  return (
    <Tag tag="finance" label="Finance" onClick={(e) => onFilter(e.label)} />
  );
}
```

## Props

<!-- props:start TagProps -->

_Generated by `pnpm readme:props` from `TagProps` in `Tag.types.ts`. Do not edit; edit the JSDoc._

| Prop               | Type                                    | Required | Default | Description                                                                                               |
| ------------------ | --------------------------------------- | -------- | ------- | --------------------------------------------------------------------------------------------------------- |
| `tag`              | `string`                                | **yes**  | –       | Identifier of the tag. It is handed to `onDelete`, and it is the text shown when `label` is left out.     |
| `className`        | `string`                                | no       | –       | Applied to the outermost element.                                                                         |
| `dataTestId`       | `string`                                | no       | –       | `data-testid` of the outermost element.                                                                   |
| `icon`             | `FC<SVGProps<SVGSVGElement>> \| string` | no       | –       | Glyph before the label: an SVG URL, or a component rendered as a 12px icon.                               |
| `iconClassName`    | `string`                                | no       | –       | Applied to that glyph.                                                                                    |
| `id`               | `string`                                | no       | –       | Applied to the outermost element.                                                                         |
| `isDefault`        | `boolean`                               | no       | –       | Ignored. Nothing reads this prop on a single tag; it is `TagType.isDefault` that `Tags` acts on.          |
| `isDeleted`        | `boolean`                               | no       | –       | Whether the tag counts as removed: `onClick` stops firing and the border greys out.                       |
| `isDisabled`       | `boolean`                               | no       | –       | Whether the tag is inert. Pointer events are dropped in CSS as well, so hover does nothing either.        |
| `isLast`           | `boolean`                               | no       | –       | Whether the trailing margin is dropped, for the last tag in a row.                                        |
| `isNewTag`         | `boolean`                               | no       | `false` | Whether the tag is drawn in its "new" colours. It is also what makes the delete cross appear.             |
| `label`            | `string`                                | no       | –       | Text of the tag. It is also the `title` attribute and the accessible name.                                |
| `labelSuffix`      | `string`                                | no       | –       | Extra text after the label, inside the same line.                                                         |
| `labelSuffixColor` | `string`                                | no       | –       | CSS colour of that suffix.                                                                                |
| `onClick`          | `(tag: TagClickEvent) => void`          | no       | –       | Called on a click anywhere in the tag, with `{ label, roomType, providerType }` — not with the DOM event. |
| `onDelete`         | `(tag?: string) => void`                | no       | –       | Called with `tag` when the cross is clicked. The cross is only rendered when `isNewTag` is set as well.   |
| `onMouseEnter`     | `() => void`                            | no       | –       | Called when the pointer enters the tag.                                                                   |
| `onMouseLeave`     | `() => void`                            | no       | –       | Called when the pointer leaves the tag.                                                                   |
| `providerType`     | `number`                                | no       | –       | Passed straight back through `onClick`. The component itself does nothing with it.                        |
| `ref`              | `RefObject<HTMLDivElement \| null>`     | no       | –       | Ref to the outermost element.                                                                             |
| `roomType`         | `number`                                | no       | –       | Passed straight back through `onClick`. The component itself does nothing with it.                        |
| `style`            | `CSSProperties`                         | no       | –       | Merged into the outermost element's inline style, before `tagMaxWidth` is applied.                        |
| `tagMaxWidth`      | `string`                                | no       | –       | `max-width` of the tag, as a CSS length — `Tags` passes a percentage of its own width.                    |
| `withLabel`        | `boolean`                               | no       | `true`  | Whether the label is rendered. Turn it off for a tag that is only its icon.                               |

<!-- props:end -->

`Tags` accepts a different, wider shape for each entry of its row:

<!-- props:start TagType -->

_Generated by `pnpm readme:props` from `TagType` in `Tag.types.ts`. Do not edit; edit the JSDoc._

| Prop               | Type                                    | Required | Default | Description                                                                                      |
| ------------------ | --------------------------------------- | -------- | ------- | ------------------------------------------------------------------------------------------------ |
| `label`            | `string`                                | **yes**  | –       | Text of the tag. `Tags` also uses it as the React key.                                           |
| `advancedOptions`  | `string[]`                              | no       | –       | Labels listed in the overflow drop-down. `Tags` fills this in for the overflow tag itself.       |
| `icon`             | `FC<SVGProps<SVGSVGElement>> \| string` | no       | –       | Glyph before the label: an SVG URL, or a component.                                              |
| `isDefault`        | `boolean`                               | no       | –       | Marks the tag as the room's default one. It only takes part in the width arithmetic.             |
| `isDisabled`       | `boolean`                               | no       | –       | Whether the tag is inert.                                                                        |
| `isOptionTag`      | `boolean`                               | no       | –       | Marks the tag as the overflow or create trigger. `Tags` sets this itself.                        |
| `isThirdParty`     | `boolean`                               | no       | –       | Marks the tag as a third-party provider: it is given a fixed 44px width and its label is hidden. |
| `key`              | `string`                                | no       | –       | React key for the tag. `Tags` falls back to the label when it is absent.                         |
| `labelSuffix`      | `string`                                | no       | –       | Extra text after the label.                                                                      |
| `labelSuffixColor` | `string`                                | no       | –       | CSS colour of that suffix.                                                                       |
| `maxWidth`         | `string`                                | no       | –       | `max-width` of the tag. `Tags` overwrites whatever is passed here with its own calculation.      |
| `onClick`          | `() => void`                            | no       | –       | Ignored by `Tags`, which wires every tag to `onSelectTag` instead.                               |
| `providerType`     | `number`                                | no       | –       | Passed back through `onSelectTag`.                                                               |
| `roomType`         | `number`                                | no       | –       | Passed back through `onSelectTag`.                                                               |

<!-- props:end -->

## Recipes

### Disabled / read-only

`isDisabled` dashes the border and drops pointer events in CSS, so hover does nothing either.

```tsx
import { Tag } from "@onlyoffice/apps-ui-kit/components/tag";

export function ArchivedTag() {
  return <Tag tag="archive" label="Archived" isDisabled />;
}
```

### Removable

The cross needs **both** `isNewTag` and `onDelete`; either alone renders no cross.

```tsx
import { useState } from "react";
import { Tag } from "@onlyoffice/apps-ui-kit/components/tag";

export function DraftTags() {
  const [tags, setTags] = useState(["design", "q3"]);

  return (
    <div style={{ display: "flex" }}>
      {tags.map((tag, index) => (
        <Tag
          key={tag}
          tag={tag}
          label={tag}
          isNewTag
          isLast={index === tags.length - 1}
          onDelete={(name) => setTags((t) => t.filter((x) => x !== name))}
        />
      ))}
    </div>
  );
}
```

### A provider tag, icon only

`withLabel={false}` leaves the glyph on its own — this is how a third-party storage is marked.

```tsx
import { Tag } from "@onlyoffice/apps-ui-kit/components/tag";

export function ProviderTag({ iconUrl }: { iconUrl: string }) {
  return (
    <Tag
      tag="provider"
      label="Nextcloud"
      icon={iconUrl}
      withLabel={false}
      providerType={3}
    />
  );
}
```

## Behaviour the types don't state

- **`onClick` receives `{ label, roomType, providerType }`, not an event.** `label` falls back
  to `tag` when you did not pass one, so the handler always has something to filter by.
- **The delete cross is drawn only when `isNewTag` is set.** `onDelete` on its own does
  nothing, which is the usual reason a tag looks un-removable.
- **`isDeleted` and `isDisabled` do not restyle the text.** The stylesheet has rules for a
  struck-through label and a greyed one, but the class they need is never put on the text, so
  only the border changes. Do not rely on either state being legible on its own.
- **`isDisabled` also blocks hover**, because the CSS sets `pointer-events: none` on the whole
  tag — a tooltip attached to it will not open.
- **The clickable area is bigger than the tag.** A pseudo-element extends it 3px on every side
  and 28px tall, so two tags closer than that overlap in their hit areas.
- **The tag brings its own trailing margin** of 4px, which `isLast` removes. In a flex row with
  a `gap` the two add up.
- **The tag is `width: fit-content` and capped at 22px tall.** `tagMaxWidth` is written into
  the inline style, after `style`, so it wins over a `maxWidth` set there.
- **A component passed as `icon` is rendered inside an [`IconButton`](../icon-button/README.md)**
  at 12px and the tag's padding drops to 0; a string is fetched and inlined instead. The two
  forms do not look the same.
- `isDefault` is declared on `TagProps` and never read — it is `TagType.isDefault`, the shape
  [`Tags`](../tags/README.md) takes, that has an effect.
- The `title` attribute and the accessible name are both `label`, so a tag with no label has
  neither.

## CSS variables

Set them on any ancestor.

| Variable              | Default     | Effect                                |
| --------------------- | ----------- | ------------------------------------- |
| `--tag-bg`            | theme token | Background of the tag.                |
| `--tag-border-style`  | theme token | Whole `border` shorthand.             |
| `--tag-radius`        | `6px`       | Corner radius.                        |
| `--tag-inner-padding` | `1px 7px`   | Inner padding.                        |
| `--tag-spacing-end`   | `4px`       | Trailing margin, removed by `isLast`. |
| `--tag-height`        | `22px`      | `max-height` of the tag.              |
| `--tag-text-color`    | theme token | Label colour.                         |
| `--tag-size`          | `13px`      | Label font size.                      |
| `--tag-lh`            | `20px`      | Label line height.                    |

## Accessibility

- The tag is a `<div>` with a click handler and `aria-label` set from `label`. It has **no
  role and no `tabIndex`**, so a clickable tag cannot be reached or activated from the keyboard.
- `aria-disabled` is set from `isDisabled`, and the CSS additionally makes the tag inert.
- The delete cross is an [`IconButton`](../icon-button/README.md), which is itself a `<div>`
  with no name — nothing announces which tag it removes, and it cannot be focused.
- A tag with `withLabel={false}` announces only its `aria-label`, so keep `label` filled in
  even when it is not drawn.

## Test ids

| Element | `data-testid`               |
| ------- | --------------------------- |
| The tag | `tag_item`, or `dataTestId` |

[`Tags`](../tags/README.md) overrides it per tag with `tag_item_<label>`.

## Related

- [`Tags`](../tags/README.md) — the row that lays these out and handles the overflow.
- [`SelectedItem`](../selected-item/README.md) — the removable chip for a user's own choice.
- [`Badge`](../badge/README.md) — the filled pill for a count or a status.
