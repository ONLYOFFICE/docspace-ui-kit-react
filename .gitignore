node_modules
dist
coverage
.idea
.DS_Store
storybook-static
# locales/en is committed so the package builds, tests and runs Storybook with
# no DocSpace checkout next to it. Every other language is produced on demand by
# `pnpm sync-locales` and stays out of git.
locales/*
!locales/en
css
fonts
debug-storybook.log
.env
.env.local